# analysis_titanic

再利用可能な Python コードを置く。ノートブックから import して使う。

```
src/analysis_titanic/
├── __init__.py
├── paths.py       # パス定数
└── load_data.py   # CSV 読み込み
```

構成を変えたら `doc/specs/05_ディレクトリ構成.md` も更新する。
