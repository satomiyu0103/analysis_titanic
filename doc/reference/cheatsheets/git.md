# Git 早見表

```bash
git status
git checkout -b feat/your-topic
git add .
git commit -m "feat: 変更内容を日本語で"
git push -u origin HEAD
```

コミットメッセージは日本語 + Conventional Commits（`doc/ai_guidelines/実装規約.md` 参照）。
