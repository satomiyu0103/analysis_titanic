# システム設計

最終更新: 2026-06-19

---

## データ

| ファイル | 行数目安 | 主な列 |
|---|---|---|
| `data/train.csv` | 445 | id, survived, pclass, sex, age, sibsp, parch, fare, embarked |
| `data/test.csv` | — | survived なし |
| `data/sample_submit.csv` | — | id, survived（提出形式） |

## パイプライン（予定）

```
load_data → preprocess → train/evaluate → predict → submit.csv
     ↑            ↑            ↑
  src/        src/       src/ + models/
```

## モジュール責務

| モジュール | 責務 |
|---|---|
| `load_data.py` | CSV 読込・列検証 |
| `preprocess.py`（予定） | 特徴量エンジニアリング |
| `train.py`（予定） | 学習・CV・モデル保存 |
| `predict.py`（予定） | 推論・提出ファイル出力 |

## ノートブック方針

- `notebooks/` で EDA と試行錯誤
- 確定した処理は `src/analysis_titanic/` へ移し、unit test を追加
